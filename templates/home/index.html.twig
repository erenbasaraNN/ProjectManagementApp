{% extends 'base.html.twig' %}

{% block title %}Ana Sayfa{% endblock %}

{% block body %}
    <div class="home-container">
        <h1>Hoş Geldiniz!</h1>
        <p>Proje yönetimi platformumuza hoş geldiniz. Başlamak için giriş yapın ya da yeni bir proje oluşturun.</p>

        <div class="actions">
            {% if app.user %}
                <a href="{{ path('project_index') }}" class="btn btn-primary">Projeleri Gör</a>
                <a href="{{ path('project_new') }}" class="btn btn-secondary">Yeni Proje Ekle</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-primary">Giriş Yap</a>
                <a href="{{ path('app_register') }}" class="btn btn-primary">Kayıt Ol</a>
            {% endif %}
        </div>
    </div>
{% endblock %}
